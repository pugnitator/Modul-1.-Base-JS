const carName = 'НИВА';
let maxCarSpeed = '142';
let carOwner = 'Доминик';

alert(carName);
alert(maxCarSpeed);
alert(carOwner);

maxCarSpeed = 150;
console.log(maxCarSpeed);

maxCarSpeed = 280;
console.log(maxCarSpeed);

maxCarSpeed = 0;
console.log(maxCarSpeed);